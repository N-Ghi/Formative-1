HOST=<localhost or IP address>
PORT=<port number for node server, e.g., 3000>
DB_NAME=<database name, e.g., exampleDB>
DB_USER=<database user, e.g., root>
DB_PASSWORD=<database password, if any>

NODE_ENV=<development or production>
JWT_EXPIRES_IN=<7d>
JWT_SECRET=<your-secret-key>

FRONTEND_URL=<http://localhost:5173 or your frontend URL>

SEED_PASSWORD=<default password for seeded users>